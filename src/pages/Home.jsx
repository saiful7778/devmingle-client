import { Button } from "keep-react";

const Home = () => {
  return (
    <Button
      className="active:focus:scale-95 duration-100"
      size="sm"
      type="default"
      typeof="submit"
    >
      Click
    </Button>
  );
};

export default Home;
